{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  },
  "scripts": {
    "dev": "lerna run dev --stream",
    "dev:website": "lerna run dev --scope=website --stream",
    "build": "lerna run build --stream"
  }
}
